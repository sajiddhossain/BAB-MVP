# Pubbliche per design: finiscono nel bundle del frontend.
# Sono sicure SOLO grazie alla RLS (athlete_id = auth.uid() su ogni tabella).
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# NON mettere qui la password del database né il personal access token:
# quelli non servono all'app e non devono stare in un file che finisce nel bundle.
