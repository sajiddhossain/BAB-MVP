.DS_Store
node_modules/
dist/
.env
.env.local

# Gli screenshot di ogni versione: ~10 MB a giro, e i binari restano nella
# storia di git anche dopo che li cancelli. Si rifanno in cinque minuti con
# `node scripts/shots.mjs` dalla cartella app/, quindi il repo non è il posto
# giusto per tenerli — e la cartella sul disco resta dov'è.
/screens/

# Il punto della situazione: documento da mandare fuori, non da versionare.
docs/05-roadmap/04-punto-della-situazione-*.md
